/* const fs = require("fs"); */

const nombre = require(".app.js");

console.log("hola" + nombre);

/* crear array de objetos con usuarios que tengan nombre y apellido en un archivo llamado usuarios.js dentro de la carpeta data
exportarla e importarla en un archivo llamado app.js que recorra el array y nos diga por consola 'hola + nombre + apellido' */
