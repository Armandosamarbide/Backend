let nombre: string = 'Juan'

console.log(nombre)

let palabra: any = 'culo'

let activo: boolean = true:

/* Le tenés que especificar qué recibe y qué devuelve, por eso ponemos number */

const calcularIva = (precio: number): number => {
    return precio * 0.21
}

/* Esta función no retorna nada, así que devuelve void
 */
const alert = (mensaje: string): void => {
    alert(mensaje)
}

